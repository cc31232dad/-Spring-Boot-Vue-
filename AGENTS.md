# 助农商城项目开发规则

## 角色与目标

- 以资深全栈工程师的标准维护本项目，优先保证业务正确性、权限安全、数据一致性、可测试性和可维护性。
- 开始修改前先阅读相关现有代码、测试、数据库迁移和交接文档，不根据文件名或页面截图猜测实现状态。
- 默认使用中文与用户沟通，所有面向用户的页面文案、提示、确认框和错误信息均使用简体中文。
- 普通、可逆、任务范围内的实现可直接完成；删除数据、破坏性迁移、重大架构调整或发布操作必须先说明影响并取得确认。

## 技术栈

- 前端：Vue 3、TypeScript、Composition API、Vue Router 4、Pinia、Axios、Vite、Vitest。
- 后端：Java 21、Spring Boot 3.5、Spring MVC、Spring Security、JWT、MyBatis Plus、Flyway。
- 数据：MySQL 8、Redis、Lua。
- 基础设施：Docker Compose。
- 当前部署形态是前后端分离的模块化单体，不擅自拆分为微服务。
- 不新增 Element Plus、Ant Design Vue 等大型 UI 依赖，除非需求明确要求且已说明成本。

## 项目目录

```text
agricultural-mall/
├── frontend/                 Vue 前端
├── backend/                  Spring Boot 后端
├── docs/                     设计、测试和交接文档
├── docker-compose.yml        MySQL 与 Redis
└── AGENTS.md                 本项目开发规则
```

### 前端目录规则

```text
frontend/src/
├── api/                      Axios 接口与 TypeScript 数据类型
├── components/               可复用组件
├── router/                   Vue Router 路由
├── stores/                   Pinia 状态管理
├── views/
│   ├── shop/                 商城、商品详情、购物车、秒杀等页面
│   ├── auth/                 登录与注册页面
│   ├── user/                 普通用户中心页面
│   ├── farmer/               农户工作台页面
│   └── admin/                管理员工作台页面
├── data/                     明确标记的演示或回退数据
└── styles.css                当前全局主题与公共样式
```

- 新页面按 `shop/auth/user/farmer/admin` 归类，不再无规则地放入 `views` 根目录。
- 现有根目录页面在专门的目录重构任务开始前可以保留，不要在业务功能提交中顺便大规模移动。
- 页面使用 `*View.vue`；可复用组件使用清晰的业务名，如 `OrderCard.vue`。
- API 请求只能集中在 `src/api`，页面不得散落重复的 Axios 调用。
- 跨页面业务状态放入 Pinia；仅当前组件使用的表单和交互状态保留在组件内部。
- 避免页面和 Store 各自维护一套不一致的同类数据。

### 后端目录规则

后端按业务模块组织：

```text
backend/src/main/java/com/agromall/
├── auth/
├── user/
├── usercenter/
├── product/
├── cart/
├── order/
├── seckill/
└── common/
```

每个模块当前采用以下分层：

```text
模块/
├── api/                      Controller、Request DTO、Response View
├── application/              Service 与用例编排
├── domain/                   领域对象、状态和核心规则
└── infrastructure/           MyBatis Mapper 与基础设施实现
```

- 保持“先按业务模块，再在模块内分层”的结构，不新增全局混杂的 `controller`、`service`、`mapper` 大目录。
- `api` 层不直接编写数据库操作；业务规则放在 `application` 或 `domain`。
- `domain` 不依赖前端展示细节。
- 数据库实体不得直接作为公开接口响应，使用 Request DTO 和 View DTO。
- 全部 API 使用统一的 `ApiResponse<T>`，包含 `code`、`message`、`data`。
- 全局异常通过统一异常处理器转换，不在每个 Controller 重复捕获。

## 核心业务规则

### 用户与权限

- `USER` 是普通消费者，管理自己的购物车、订单、地址、收藏和资料。
- `FARMER` 是农户，只能管理自己的商品和相关订单。
- `ADMIN` 是管理员，可以进行审核和全局管理。
- 普通注册只能授予 `USER`，禁止通过公开注册接口创建 `FARMER` 或 `ADMIN`。
- 前端路由守卫用于改善体验，真正的权限校验必须由 Spring Security 和后端业务所有权检查完成。
- 用户只能访问和修改属于自己的订单、地址、收藏等资源。

### 商品

- 目标商品流程：农户提交商品 -> `PENDING_REVIEW` -> 管理员审核 -> `ON_SALE` 或 `REJECTED`。
- 只有 `ON_SALE` 商品可以通过公开商品接口展示给普通用户。
- 农户不得直接把待审核或已拒绝商品设置为上架。
- 管理员拒绝商品必须保存拒绝原因；农户应能查看原因并修改后重新提交。
- 商品库存、价格和状态以数据库和后端校验为准，不能信任前端传值。
- 模拟商品仅在真实商品目录为空时作为演示回退；真实商品存在时必须优先展示真实数据。

### 购物车与订单

- 加入购物车、结算和订单状态转换均由后端校验用户、商品状态、库存和所有权。
- 库存扣减、订单创建和购物车清理需要保持事务一致性。
- 订单取消、发货、确认收货只能在允许的状态下执行。
- 用户地址由地址接口提供，结算页应优先使用默认地址并允许切换。

### 秒杀

- Redis Lua 脚本负责原子库存检查、扣减和一人一单控制。
- 数据库订单创建失败时必须补偿 Redis 预留。
- 不用普通的“先查询再扣减”替代 Lua 原子操作。

### 收藏与用户中心

- 收藏状态必须由后端持久化，不能只保存在浏览器内存。
- 商品详情和用户中心收藏列表必须保持状态一致。
- 用户资料、密码和手机号修改需要服务端验证；密码不得明文存储或返回。

## 前端视觉规则

- 保持绿色主色与米白、浅绿背景，整体温暖、简洁、适合农产品电商。
- 商城、购物车和用户中心应使用统一视觉语言，不把用户中心设计成完全不同的后台系统。
- 所有交互适配移动端；固定格式区域使用稳定的网格、最小宽度和响应式断点。
- 避免卡片套卡片、巨型营销 Hero、纯装饰渐变和不必要的大圆角。
- 按钮、输入框和状态需要可见的加载、禁用、成功、错误和空状态。
- 删除地址、取消订单、审核拒绝等高影响操作需要二次确认。
- 不显示没有真实数据来源的红点、未读数量、销量或其他误导性状态；演示数据必须明确标记。

## 编码与接口规范

- TypeScript 类型要明确，避免无理由使用 `any`。
- Vue 使用 Composition API；复杂页面拆分为职责清晰的组件。
- Java 请求参数使用 Bean Validation，并在 Service 层再次校验业务条件。
- REST 路径使用资源名和明确动作，保持现有接口兼容，变更已有合同前检查所有调用方。
- 金额使用 `BigDecimal`；不能用浮点数处理后端金额。
- 时间、状态枚举、手机号脱敏和分页规则在前后端保持一致。
- 中文源文件使用 UTF-8，发现乱码时修复源文件，不用英文临时替代中文需求。
- 注释只解释不明显的业务原因或复杂逻辑，不写重复代码含义的注释。

## 工作流程

1. 阅读相关代码、测试、迁移和 `docs/handoff/next-work-handoff.md`。
2. 说明将修改的业务边界和关键假设。
3. 优先编写或更新能够证明目标行为的测试。
4. 以最小必要范围实现前后端闭环，不留下只有页面没有后端或只有接口没有入口的半成品。
5. 运行与风险匹配的测试、构建和代码检查。
6. 汇报完成内容、验证结果、剩余风险和使用方式。
7. 阶段结束时更新交接文档。

## 完成标准

- 用户请求涉及真实业务数据时，确认前端、API、Service、Mapper 和数据库是否形成完整闭环。
- 新增或修改后端接口必须覆盖成功、权限不足、资源不属于当前用户、非法状态或非法参数等关键情况。
- 新增前端 Store 或关键交互应有 Vitest 测试。
- 不允许以“页面能打开”代替功能完成。
- 不允许在没有运行验证命令时声称测试通过或功能完成。

## 验证命令

后端：

```powershell
cd backend
mvn test
```

前端：

```powershell
cd frontend
npm run test -- --run
npm run build
```

提交前：

```powershell
git diff --check
git status --short
```

若全量测试因本机残留测试数据失败，应先确认根因并修复测试隔离或清理明确的测试数据，不得简单忽略失败。

## Git 与文件安全

- 当前功能分支为 `feature/phase-3-cart-orders`；除非用户要求，不切换或重写分支历史。
- 不提交 `.env`、`.vscode/`、`frontend/node_modules/`、`frontend/dist/`、`backend/target/` 或本地数据库数据。
- 不使用 `git reset --hard`、强制推送或其他破坏性 Git 命令，除非用户明确授权。
- 提交只包含当前任务相关文件，提交信息描述真实业务变化。
- 现有 PR 会自动包含推送到当前分支的新提交。

## 当前优先级

按照 `docs/handoff/next-work-handoff.md` 推进：

1. P0：商品待审核状态、管理员审核 API、管理员审核页、农户商品管理页和角色测试流程。
2. P1：商品详情收藏、账户安全、结算页地址选择和订单体验。
3. P2：图片上传、支付、物流、评价、售后和管理员统计。

不要为了视觉细节跳过 P0 的商品审核主流程和权限测试。
